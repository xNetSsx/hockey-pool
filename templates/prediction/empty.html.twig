{% extends 'base.html.twig' %}

{% block title %}Tipy — Hockey Pool{% endblock %}

{% block body %}
    <div class="flex min-h-[50vh] items-center justify-center">
        <div class="text-center">
            <div class="mb-4 text-6xl">🏒</div>
            <h1 class="text-2xl font-bold text-gray-700 dark:text-gray-200">Žádný turnaj</h1>
            <p class="mt-2 text-gray-500 dark:text-gray-400">Zatím není co tipovat.</p>
            <a href="{{ path('tournament_archive') }}" class="mt-4 inline-block rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700 dark:bg-[#38bdf8] dark:text-[#0f172a]">Podívej se na archiv turnajů</a>
        </div>
    </div>
{% endblock %}
