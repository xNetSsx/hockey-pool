{% extends 'admin/layout.html.twig' %}

{% block title %}Výsledky speciálních tipů — Admin{% endblock %}

{% set inputClass = 'w-full rounded-lg border border-gray-300 px-4 py-2.5 text-gray-900 focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500/20 dark:bg-[#0f172a] dark:border-gray-600 dark:text-white' %}

{% block admin_body %}
    <div class="mx-auto max-w-2xl">
        <h1 class="mb-4 text-xl font-bold dark:text-white">Zadat výsledky — {{ tournament.name }}</h1>

        {% if rules|length == 0 %}
            <div class="rounded-xl bg-white p-8 text-center shadow dark:bg-[#1e293b] dark:shadow-gray-900/30">
                <p class="text-gray-500 dark:text-gray-400">Žádná pravidla. Nejprve <a href="{{ path('admin_rules') }}" class="text-blue-600 hover:underline dark:text-[#38bdf8]">vytvoř pravidla</a>.</p>
            </div>
        {% else %}
            <form method="post" class="space-y-6">
                <input type="hidden" name="_token" value="{{ csrf_token('special_results') }}">

                <div class="overflow-hidden rounded-xl bg-white shadow dark:bg-[#1e293b] dark:shadow-gray-900/30">
                    <div class="border-b bg-[#0f172a] px-6 py-3 dark:border-gray-700">
                        <h2 class="font-semibold text-white">Skutečné výsledky</h2>
                    </div>
                    <div class="divide-y divide-gray-100 dark:divide-gray-700">
                        {% for rule in rules %}
                            <div class="px-6 py-4">
                                <label for="rule_{{ rule.id }}" class="mb-1 block text-sm font-medium text-gray-700 dark:text-gray-300">
                                    {{ rule.name }}
                                    <span class="text-xs text-gray-400 dark:text-gray-500">({{ rule.valueType.label }}, {{ rule.points }} b.)</span>
                                </label>

                                {% if rule.valueType.value == 'team' %}
                                    <select name="rule_{{ rule.id }}" id="rule_{{ rule.id }}" class="{{ inputClass }}">
                                        <option value="">— Vyber tým —</option>
                                        {% for team in teams %}
                                            <option value="{{ team.id }}" {{ rule.actualTeamValue and rule.actualTeamValue.id == team.id ? 'selected' : '' }}>
                                                {{ team.flagEmoji }} {{ team.name }}
                                            </option>
                                        {% endfor %}
                                    </select>
                                {% elseif rule.valueType.value == 'string' %}
                                    <input type="text" name="rule_{{ rule.id }}" id="rule_{{ rule.id }}"
                                           value="{{ rule.actualStringValue }}"
                                           class="{{ inputClass }}">
                                {% elseif rule.valueType.value == 'integer' %}
                                    <input type="number" name="rule_{{ rule.id }}" id="rule_{{ rule.id }}"
                                           value="{{ rule.actualIntValue is not null ? rule.actualIntValue : '' }}"
                                           class="{{ inputClass }}" min="0">
                                {% endif %}
                            </div>
                        {% endfor %}
                    </div>
                </div>

                <div class="text-center">
                    <button type="submit" class="rounded-lg bg-blue-600 px-8 py-2.5 font-semibold text-white hover:bg-blue-700 dark:bg-[#38bdf8] dark:text-[#0f172a] dark:hover:bg-[#7dd3fc]">
                        Uložit a přepočítat speciální tipy
                    </button>
                </div>
            </form>
        {% endif %}
    </div>
{% endblock %}
